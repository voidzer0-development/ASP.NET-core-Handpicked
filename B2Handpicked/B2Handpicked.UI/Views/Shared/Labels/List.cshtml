@model IEnumerable<B2Handpicked.DomainModel.Label>

<table class="mdl-data-table mdl-js-data-table mdl-shadow--2dp">
    <thead>
        <tr>
            <th class="mdl-data-table__cell--non-numeric">
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th class="mdl-data-table__cell--non-numeric">
                @Html.DisplayNameFor(model => model.Token)
            </th>
            <th class="mdl-data-table__cell--non-numeric">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td class="mdl-data-table__cell--non-numeric">
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td class="mdl-data-table__cell--non-numeric">
                    @Html.DisplayFor(modelItem => item.Token)
                </td>
                <td class="mdl-data-table__cell--non-numeric">
                    <a asp-controller="Labels" asp-action="Details" asp-route-id="@item.Id"><i class="material-icons">info</i></a>
                    <a asp-controller="Labels" asp-action="Edit" asp-route-id="@item.Id"><i class="material-icons">edit</i></a>
                    <a asp-controller="Labels" asp-action="Delete" asp-route-id="@item.Id"><i class="material-icons">delete</i></a>
                </td>
            </tr>
        }
    </tbody>
</table>